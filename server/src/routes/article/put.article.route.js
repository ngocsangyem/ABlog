import express from "express";
const router = express.Router();

/* PUT update an existing api in database */
const deleteArticle = router.put("/", (req, res, next) => {});

export default deleteArticle;
